package com.logic.map;

import com.proto.map.gen.WorldMapMsg.ConfigNodeProto;

/**
 * 地图节点
 * 
 * @author dream
 *
 */
public class MapNode
{
    private int nodeID;

    /** 节点列号 */
    private int nx;

    /** 节点行号 */
    private int ny;

    private int px;

    private int py;

    private boolean isCanMove;
    
    private int line;

    public MapNode(ConfigNodeProto config)
    {
        nodeID = config.getNodeID();
        nx = config.getNodeX();
        ny = config.getNodeY();
        isCanMove = config.getIsCanMove();
        px = config.getPixelX();
        py = config.getPixelY();
    }

    public void init(ConfigNodeProto config)
    {
        
    }

    public boolean getCanMove()
    {
        return isCanMove;
    }

    public int getPX()
    {
        return px;
    }

    public int getPY()
    {
        return py;
    }

    public int getNodeX()
    {
        return nx;
    }

    public int getNodeY()
    {
        return ny;
    }

    public int getNodeID()
    {
        return nodeID;
    }
}
