package com.logic.map;

public class PathFindFactory
{
    public static MapFindPath findPath(int start, int end, MapGraph map)
    {
        PathAStar path=new PathAStar(map);
        return path.findPath(start, end);
    }
}
